commands/plan.md:30:6. **Sign-off.** Read the finished draft back in brief. A board preview is still available during the dialogue (write the draft to `plans/execution/<NN-slug>/.draft-v1.md` and run `/research-plans:board <NN-slug>`; fold annotations into the draft), but **formal approval happens at the sign-off gate**: writing `v1.md` automatically opens the plan in the researcher's browser, where they approve or request changes. **Preserve the drafting trail:** each time you revise the draft for a fresh review round, first snapshot the *current* `.draft-v1.md` to the next `plans/execution/<NN-slug>/v1-draft-<K>.md` (K from 1, the next unused number — never overwrite a snapshot). These commit as the plan's iteration history and are read-only on the board; the sign-off gate ignores them (only `vN.md` is gated). Before writing `v1.md`, make sure any preview board session has been submitted or closed — the gate needs the board. Add the sign-off line with the researcher's name and today's date, then write `v1.md`. If the gate denies, read `plans/.board-feedback.md`, revise the draft to address all feedback, and write again (the gate reopens). If the gate **times out** instead (the researcher wasn't at the browser), the proposal is saved back as the component's `.draft-vN.md`: relaunch `/research-plans:board <NN-slug>` and tell the researcher the draft awaits their Approve there — approving mints a durable ticket and the board workflow's routing performs the ticketed write; never bypass the gate and never use `--gate-batch` for this. After approval lands: delete only the ephemeral `.draft-v1.md` (the committed `v1-draft-*.md` snapshots stay as history), update the component's tracker row to `planned` with a link to the plan, and update `Last updated:`.
commands/board.md:32:   - **Sign-off order** (`## SIGNOFF: <component> v<N> — <decision>`; `signoff` object in the fence — the researcher clicked Approve or Request changes on a plan draft from the Tracker or Plan view): the board server has ALREADY validated the request against the exact draft it displayed and, for `approve`, written a one-use approval ticket (`plans/execution/.import-approved-<slug>-v<N>`). Your job:
commands/board.md:33:     - **approve** → write the signed `v<N>.md` from `.draft-v<N>.md` exactly as the gate-approve path does: copy the draft content verbatim, append the `Signed off: <git user.name>, <date>` line, delete the ephemeral `.draft-v<N>.md` (keep any committed `v<N>-draft-*.md` snapshots). The sign-off hook admits the write by consuming the ticket. **NEVER hand-write a ticket yourself** — the hook denies agent-written tickets as forgery; if the ticket is missing, treat the order as request-changes and tell the researcher why. Then update the tracker row (planned → in progress, plan link) as after any sign-off, run the `/research-plans:review` workflow on the newly-signed `v<N>.md` to refresh its score (an idempotent, non-blocking save — see review.md step 4), and log the decision.
skills/managing-research-plans/SKILL.md:46:- **Versions are immutable — and mechanically enforced.** `v1.md, v2.md, ...` are never overwritten or edited; the plugin's sign-off gate (a PreToolUse hook) blocks edits to signed versions and opens a browser approval for every new version write. Approval happens on the board, not in the terminal. If the gate denies, read `plans/.board-feedback.md`, revise, and write again. If it times out, the proposal is saved as the component's `.draft-vN.md` — recovery is the researcher's Approve on the persistent board (mints the same durable ticket), never the bypass. Batch sign-off (`board.py --gate-batch`) is the `/adopt` bulk flow only and refuses fewer than 2 pending drafts (`--allow-single` is the explicit exception). Headless/CI sessions set `RESEARCH_PLANS_NO_GATE=1` (the bypass leaves a stderr trace). Deviations are recorded, never hidden — especially the improvised ones.
skills/managing-research-plans/scripts/signoff_gate.py:32:TICKET_PREFIX = ".import-approved-"
skills/managing-research-plans/scripts/signoff_gate.py:230:    # ---- Batch-approval ticket forgery guard. Tickets (.import-approved-*) are
