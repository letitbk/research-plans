=== scorecard schema (schemaVersion 3 / board-scorecard) ===
commands/review.md:7:Score one execution plan against the rubric at `${CLAUDE_PLUGIN_ROOT}/skills/managing-research-plans/references/plan-rubric.md` (v0.4, five channels, 0–3 each). There is no pass/fail threshold — a plan is scored, not gated. Requires an initialized project; if `plans/master-plan.md` is absent, say so and stop.
commands/review.md:11:2. **Dispatch or run inline.** When `.claude/agents/rp-plan-reviewer.md` exists, the scoring runs in ONE dispatched `rp-plan-reviewer` subagent — its model and effort come from `plans/model-profile.md`, a request the platform can override (org allowlist, `CLAUDE_CODE_SUBAGENT_MODEL`); when it does not exist, run step 3 inline exactly as written (no anonymous-subagent fallback). Before dispatching, run `python3 ${CLAUDE_PLUGIN_ROOT}/skills/managing-research-plans/scripts/models.py check` and relay its one-line hint verbatim if it prints one (advisory — proceed with the existing agent). Dispatch one Task with `subagent_type: "rp-plan-reviewer"`, NO model override, giving it: the plan path, the master plan path, `plans/decision-log.md`, the component's execution directory, the rubric path, the split-criteria path (`${CLAUDE_PLUGIN_ROOT}/skills/managing-research-plans/references/split-criteria.md`), the rubric version to stamp (`0.4`), today's date (`date +%Y-%m-%d`), and the step-1 resolution. The agent returns ONLY the scorecard JSON (the `board-scorecard` fence shape, schemaVersion 3). Validate it: parseable JSON; `status` ∈ `scored`/`unscorable`; a `scored` card has exactly the five channels (`goal`, `decisions`, `steps`, `validation`, `boundaries`) with integer `score` 0–3 and a `total` equal to their sum. Repair or re-prompt once on malformed output; still malformed → say so and run step 3 inline.
commands/review.md:13:3. **Score inline (skipped when step 2 dispatched).** Apply the rubric exactly. First the **readability precondition**: if the five channels' content cannot be extracted from the plan at all, report `Status: unscorable — <what is missing>` and STOP (do not fabricate scores). Otherwise score each of the five channels 0–3 with a short quoted evidence span, following the anchors. Score **from the plan itself** — not the chat or decision log. For channel 2, credit a choice among options, an approval, or joint authorship as full control; score the depth of the reasoning and its link to the research goal, not who phrased it. Then report: the **profile** (`G · D · S · V · B = <total>/15`), the **biggest leak** (the lowest channel, "where the most authorship is being handed to the agent"), the **unresolved forks** (the open decisions dragging the score down — the fix list for the next revision), **one suggested move per leak**, the non-scored **integrity flags** from step 1, and the **split assessment** per `${CLAUDE_PLUGIN_ROOT}/skills/managing-research-plans/references/split-criteria.md`. Read the profile, not the total: name any channel scoring 0 as a missing control channel.
commands/review.md:15:4. **Save the scorecard** to `plans/reviews/<NN-slug>-v<N>.md` using the template at `${CLAUDE_PLUGIN_ROOT}/skills/managing-research-plans/templates/review-scorecard.md` (schemaVersion 3; prose and JSON must agree — the board renders the JSON). Add a `"modelUsage"` field: `{"prescribed": <the `plan-review` row from `models.py stage plan-review` as {"model","effort"}, or null>, "reported": {"model": "<the dispatched agent's pinned model, or the session model inline>", "effort": <the pinned effort, or null inline>}}` — the board shows it as a "reviewed by" chip. **Idempotent, non-blocking save:** when this command is invoked automatically after a sign-off (by `/plan`, `/sync`, `/adopt`, or a board Approve), save without prompting; if a valid v3 scorecard already exists for this exact `planPath`, skip re-scoring; if scoring failed, write nothing and report "score unavailable" — never block the sign-off. When invoked interactively and the file exists, ask before overwriting. The score appears in the plan's header on the board (`/research-plans:board`).
skills/managing-research-plans/templates/review-scorecard.md:32:```json board-scorecard
skills/managing-research-plans/templates/review-scorecard.md:33:{"schemaVersion": 3, "status": "scored", "component": "<NN-slug>", "planVersion": <N>,
skills/managing-research-plans/templates/review-scorecard.md:50:<An unreadable plan uses instead: {"schemaVersion": 3, "status": "unscorable", "component": "<NN-slug>", "planVersion": <N>, "planPath": "…", "rubricVersion": "0.4", "date": "<YYYY-MM-DD>", "reason": "<what cannot be extracted and how to fix it>"} — no channels, no total, no profile.>
skills/managing-research-plans/references/plan-rubric.md:1:# Plan Rubric — five channels
skills/managing-research-plans/references/plan-rubric.md:9:The rubric scores **five channels, each 0–3**. Each channel is one place where authorship and control can leak from the human to the agent. There is **no pass/fail threshold** — a plan is scored, not gated. The score is never the deliverable: the rubric produces a **diagnosis** (a five-number profile, the biggest leak, the open forks to fix) that points at the next revision.
skills/managing-research-plans/references/plan-rubric.md:15:Before scoring, check that all five channels' content (goal & success, decisions, steps, validation, boundaries) can be extracted from the plan at all. If it cannot — the plan is unintelligible without the session that produced it — return **unscorable: fix readability first**, naming what cannot be extracted, and do **not** fabricate channel scores. This is a narrow extractability gate, not style policing; brevity and plain language are an authoring concern, not scored here.
skills/managing-research-plans/references/plan-rubric.md:17:## The five channels (0–3 each)
skills/managing-research-plans/references/plan-rubric.md:56:Sum the five channels for a 0–15 total, but **read the profile, not the total**. A missing channel must not hide behind a strong total: `3·3·3·0·3 = 12/15` is not "80% good," it is a plan with no validation. So the five-number profile is the headline, and **any channel scoring 0 is flagged as a missing control channel**. There is no band or headline percentage that lets one channel's strength cover another's absence.
skills/managing-research-plans/references/plan-rubric.md:74:The review returns one `board-scorecard` JSON object, schemaVersion 3 — see `templates/review-scorecard.md` for the exact shape. In brief: `status: "scored"` carries the five `channels` (ids `goal`/`decisions`/`steps`/`validation`/`boundaries`, integer `score` 0–3, `evidence`, `justification`), the `total`/`max`, the `profile` string, `biggestLeak`, `suggestedMoves`, `unresolvedForks`, `integrityFlags`, and the `split`. `status: "unscorable"` carries only a `reason`. No threshold block, no percent, no band.
skills/managing-research-plans/templates/agents/rp-plan-reviewer.md:3:description: Single-pass execution-plan review — scores five channels 0–3 and returns the scorecard JSON. Dispatched by /research-plans:review with the plan path, rubric path, and evidence paths.
skills/managing-research-plans/templates/agents/rp-plan-reviewer.md:10:You review ONE execution plan version and score it against the rubric's five channels. The dispatching command gives you: the plan path, the master plan path, the decision-log path, the component's execution directory, the plugin's rubric and split-criteria file paths, the rubric version to stamp, and today's date. Read the rubric first and follow it exactly — its five channels (goal & success, decisions & reasons, steps, validation, boundaries), their 0–3 anchors, the readability precondition, and the not-scored items are the entire standard; do not substitute your own.
skills/managing-research-plans/templates/agents/rp-plan-reviewer.md:14:- **Readability first.** If the five channels' content cannot be extracted from the plan at all, return the `unscorable` shape (below) — do not fabricate scores.
skills/managing-research-plans/templates/agents/rp-plan-reviewer.md:19:Your final message is consumed by the dispatching session, not read by a person. Return ONLY one JSON object — no prose before or after — shaped exactly like the board-scorecard fence (schemaVersion 3).
skills/managing-research-plans/templates/agents/rp-plan-reviewer.md:23:{"schemaVersion": 3, "status": "scored", "component": "<NN-slug>", "planVersion": <N>, "planPath": "plans/execution/<NN-slug>/v<N>.md", "rubricVersion": "<from dispatch prompt>", "date": "<YYYY-MM-DD from dispatch prompt>", "channels": [{"id": "goal", "name": "Goal & success", "score": 0|1|2|3, "evidence": "<quote>", "justification": "<one line>"}, {"id": "decisions", "name": "Decisions & reasons", "score": 0|1|2|3, "evidence": "<quote>", "justification": "<one line>"}, {"id": "steps", "name": "Steps", "score": 0|1|2|3, "evidence": "<quote>", "justification": "<one line>"}, {"id": "validation", "name": "Validation", "score": 0|1|2|3, "evidence": "<quote>", "justification": "<one line>"}, {"id": "boundaries", "name": "Boundaries", "score": 0|1|2|3, "evidence": "<quote>", "justification": "<one line>"}], "total": <sum 0-15>, "max": 15, "profile": "G<0-3>·D<0-3>·S<0-3>·V<0-3>·B<0-3>", "biggestLeak": {"channel": "<id of the lowest channel>", "note": "<where the most authorship is handed to the agent>"}, "suggestedMoves": ["<one concrete move per leak>"], "unresolvedForks": ["<the specific open decisions dragging the score down>"], "integrityFlags": [{"id": "uncommitted"|"unsupported-sources"|"unrecorded-deviation", "note": "<why>"}], "split": {"verdict": "right-sized"|"split required", "detail": "<one paragraph per split-criteria.md>"}}
skills/managing-research-plans/templates/agents/rp-plan-reviewer.md:25:Rules for the scored shape: exactly the five channels in that order, each `score` an integer 0–3; `total` is their sum; `profile` uses the fixed letters G·D·S·V·B; `integrityFlags` is `[]` when the plan is clean; `split` and `biggestLeak` and `suggestedMoves` are always present.
skills/managing-research-plans/templates/agents/rp-plan-reviewer.md:29:{"schemaVersion": 3, "status": "unscorable", "component": "<NN-slug>", "planVersion": <N>, "planPath": "plans/execution/<NN-slug>/v<N>.md", "rubricVersion": "<from dispatch prompt>", "date": "<YYYY-MM-DD from dispatch prompt>", "reason": "<what cannot be extracted and how to fix it>"}
