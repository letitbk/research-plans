=== board lifecycle: board.md vs docs/reference.md (the confirmed drift) ===
commands/board.md:15:4. **Serve (open → act → close).** The live board opens at one bookmarkable, per-project URL and has **no idle timeout** — it stays open until the researcher acts or closes it (like Plannotator). When the researcher submits an action, the board hands the work to you and **closes**; you route the action and, unless it produced something to show back (a review's seeded comments, a regenerated report) or the board must recover (a stale payload), you do **not** relaunch it — you tell the researcher the board closed and that `/research-plans:board` reopens it at the same URL. First launch:
commands/board.md:17:   **in the background** if the harness supports background bash (completion re-invokes you with its output); otherwise run it in the foreground with a 10-minute `--timeout` and say there is no reopen loop in foreground mode. The server picks a stable per-project port (41000–41999) and records `{pid, port, bootId}` in `plans/.board.lock`. Tell the researcher: the board is open at this bookmarkable URL — annotate, approve, request changes, or request reviews directly on it; it closes when they submit, and `/research-plans:board` reopens it at the same URL whenever they want. Any reopen reuses the first launch's port and skips the browser: append `--port <that port> --no-open` (read the port from `plans/.board.lock` or the launch's `Board:` line). Handle each exit:
commands/board.md:22:   - Live serving no longer times out on its own (**exit 2** is the sign-off gate's timeout now; a `--gate-batch` timeout instead exits 0 with its approvals persisted as tickets), so a plain live board never sleeps. If the bash call itself timed out or its output is lost, run `--collect` (a non-destructive peek); if it prints an order, route it (step 5), `--ack`, and do not relaunch (the board is closed) unless it was a review/report order.
commands/board.md:47:       - **`gemini`** — `agy --model "Gemini 3.1 Pro (High)" --print-timeout 20m -p "$(cat plans/.rp-review-<slug>.txt)" < /dev/null > plans/.rp-review-out.txt 2>&1` — **without** `--dangerously-skip-permissions` (the prompt is self-contained, so the model needs no repo access or write). Parse the LAST balanced JSON object from `plans/.rp-review-out.txt`.
commands/board.md:58:6. **Close the loop.** The board closes when the researcher submits an action; after routing it (and unless it was a review/report order that reopened to show its result), the board stays closed — offer `/research-plans:board` to reopen it at the same URL when they want to keep going. Same after a cancel (exit 130). If artifacts changed, suggest a commit (do not run it without approval).
docs/reference.md:50:**Live** — `/research-plans:board` starts a small local server (`python3` only, nothing to install) on a stable per-project port. Bookmark the URL; it stays valid for the whole session. The live board is a control surface: your feedback panel docks side by side with the content, and approve / request-changes / review buttons are always on hand in the Tracker, Plan, and Results views. Select text to attach a comment, act on a plan, or press "Send to Claude" — your session applies each action and the board refreshes itself with the updated state. After an idle hour it goes to sleep; `/research-plans:board` wakes it at the same URL.
