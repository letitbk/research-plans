=== substantive-finding rule ===
commands/report.md:11:   **Null-result gate.** If the bundle has no substantive findings — no metric is `robust`/`marginal` and none carries a claim `statement`, so descriptive-only counts, retracted, and superseded metrics do not qualify — do **not** write a report. Say plainly that the bundle has no substantive findings to report, and stop without creating any file. The Result tab still carries the bundle's evidence and validation; the board's Report tab shows the null-result state. This gate holds identically for every entry path — a standalone `/research-plans:report`, the results-chained offer, and the board's Generate report button — so a null result never yields an empty narrative.
skills/managing-research-plans/scripts/results.py:180:def is_substantive(metric):
skills/managing-research-plans/scripts/results.py:181:    """A metric is a substantive finding when its status is robust/marginal, or
skills/managing-research-plans/scripts/results.py:185:    `isSubstantive` (Python/TypeScript duplication — change both)."""
skills/managing-research-plans/scripts/results.py:193:def has_substantive_findings(manifest):
skills/managing-research-plans/scripts/results.py:194:    return any(is_substantive(m) for m in (manifest.get("metrics") or []))
skills/managing-research-plans/scripts/results.py:246:                 if is_substantive(mt) and not (mt.get("artifactIds") or [])]
skills/managing-research-plans/scripts/results.py:250:        "detail": ("every substantive finding cites an artifact" if not unsourced
board/src/lib/findings.ts:6:// count, a retracted claim, or a superseded one is not a substantive result.
board/src/lib/findings.ts:9:// A metric is a substantive finding when its status is robust/marginal, OR it
board/src/lib/findings.ts:12:// with neither does not. Kept in sync with results.py `is_substantive`
board/src/lib/findings.ts:14:export function isSubstantive(metric: Metric): boolean {
board/src/lib/findings.ts:20:// True when the bundle's manifest parsed and any metric is substantive. Returns
board/src/lib/findings.ts:22:// "no substantive findings" from "manifest absent" should gate on
board/src/lib/findings.ts:29:  return m.metrics.some(isSubstantive);
