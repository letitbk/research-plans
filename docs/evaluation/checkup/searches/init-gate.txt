=== initialized-project gate ===
commands/review.md:7:Score one execution plan against the rubric at `${CLAUDE_PLUGIN_ROOT}/skills/managing-research-plans/references/plan-rubric.md` (v0.4, five channels, 0–3 each). There is no pass/fail threshold — a plan is scored, not gated. Requires an initialized project; if `plans/master-plan.md` is absent, say so and stop.
commands/adopt.md:9:**Requires an initialized project** (`plans/master-plan.md` with its marker). If the repo has substantial history but is not initialized, run `/research-plans:init` first in its **minimal mode** (context + research questions + markers + real-time `Initialized:` stamp; leave the components table to this command) so the component-derivation interview happens exactly once, here. This command never narrates repository history as components — it derives research *activities* (like init) and uses evidence only to know which activities happened, their status, and when. Adopt is backward-looking: it documents work under the CURRENT research direction. To change the direction itself — a new target and new questions over the same repo — use `/research-plans:renew`, which archives the master plan, writes a fresh one, and hands any carried rows that need retrospective plans back to this command (step 1's hand-built-tracker path consumes them as-is).
commands/plan.md:7:Author an execution plan for one component, jointly with the researcher. Skill context: `${CLAUDE_PLUGIN_ROOT}/skills/managing-research-plans/SKILL.md`. This command requires an initialized project (`plans/master-plan.md` with its marker); if absent, say so and stop — suggest `/research-plans:init`.
commands/sync.md:6:Reconcile the plan artifacts with what actually happened. Skill context: `${CLAUDE_PLUGIN_ROOT}/skills/managing-research-plans/SKILL.md`. Requires an initialized project (`plans/master-plan.md` with its marker); if absent, say so and stop.
commands/models.md:6:Manage `plans/model-profile.md` — which Claude model each research-plans stage runs on. Script: `${CLAUDE_PLUGIN_ROOT}/skills/managing-research-plans/scripts/models.py` (python3, stdlib only). Requires an initialized project (`plans/master-plan.md` with its marker); if absent, say so and stop.
commands/results.md:7:Capture results for review on the board. Skill context: `${CLAUDE_PLUGIN_ROOT}/skills/managing-research-plans/SKILL.md`. Mechanics script: `${CLAUDE_PLUGIN_ROOT}/skills/managing-research-plans/scripts/results.py` (python3 only). Requires an initialized project (`plans/master-plan.md` with its marker); if absent, say so and stop.
commands/report.md:7:Assemble a standalone, collaborator-readable report for one results bundle. Skill context: `${CLAUDE_PLUGIN_ROOT}/skills/managing-research-plans/SKILL.md`. Requires an initialized project (`plans/master-plan.md` with its marker); if absent, say so and stop. Reports are **derived documents**: they live in `plans/reports/` (committed, never gitignored), regenerating overwrites, and nothing about them is immutable — the immutable record is the bundle itself.
commands/models.md:6:Manage `plans/model-profile.md` — which Claude model each research-plans stage runs on. Script: `${CLAUDE_PLUGIN_ROOT}/skills/managing-research-plans/scripts/models.py` (python3, stdlib only). Requires an initialized project (`plans/master-plan.md` with its marker); if absent, say so and stop.
commands/plan.md:7:Author an execution plan for one component, jointly with the researcher. Skill context: `${CLAUDE_PLUGIN_ROOT}/skills/managing-research-plans/SKILL.md`. This command requires an initialized project (`plans/master-plan.md` with its marker); if absent, say so and stop — suggest `/research-plans:init`.
commands/adopt.md:9:**Requires an initialized project** (`plans/master-plan.md` with its marker). If the repo has substantial history but is not initialized, run `/research-plans:init` first in its **minimal mode** (context + research questions + markers + real-time `Initialized:` stamp; leave the components table to this command) so the component-derivation interview happens exactly once, here. This command never narrates repository history as components — it derives research *activities* (like init) and uses evidence only to know which activities happened, their status, and when. Adopt is backward-looking: it documents work under the CURRENT research direction. To change the direction itself — a new target and new questions over the same repo — use `/research-plans:renew`, which archives the master plan, writes a fresh one, and hands any carried rows that need retrospective plans back to this command (step 1's hand-built-tracker path consumes them as-is).
commands/results.md:7:Capture results for review on the board. Skill context: `${CLAUDE_PLUGIN_ROOT}/skills/managing-research-plans/SKILL.md`. Mechanics script: `${CLAUDE_PLUGIN_ROOT}/skills/managing-research-plans/scripts/results.py` (python3 only). Requires an initialized project (`plans/master-plan.md` with its marker); if absent, say so and stop.
commands/sync.md:6:Reconcile the plan artifacts with what actually happened. Skill context: `${CLAUDE_PLUGIN_ROOT}/skills/managing-research-plans/SKILL.md`. Requires an initialized project (`plans/master-plan.md` with its marker); if absent, say so and stop.
commands/board.md:9:1. **Gate.** Requires `plans/master-plan.md` with its marker; if absent, say so and suggest `/research-plans:init`. Stop.
commands/renew.md:9:1. **Preflight.** Resolve the project root (as in `/research-plans:init` step 1: `git rev-parse --show-toplevel`, confirm with the researcher if it differs from the current directory). Detect state: **initialized** (`plans/master-plan.md` with its marker) or **uninitialized** (an exploratory repo). If initialized, suggest committing the current `plans/` state first — the archive move is about to restructure it. `$ARGUMENTS`, if provided, seeds the new direction; confirm rather than re-ask.
commands/report.md:7:Assemble a standalone, collaborator-readable report for one results bundle. Skill context: `${CLAUDE_PLUGIN_ROOT}/skills/managing-research-plans/SKILL.md`. Requires an initialized project (`plans/master-plan.md` with its marker); if absent, say so and stop. Reports are **derived documents**: they live in `plans/reports/` (committed, never gitignored), regenerating overwrites, and nothing about them is immutable — the immutable record is the bundle itself.
