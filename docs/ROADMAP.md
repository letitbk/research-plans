# Roadmap — feature ideas

Ideas noted for later implementation. Not commitments; each becomes real work only when scoped.

- **Auto-open the board after init** (2026-07-06, BK): when `/research-plans:init` finishes creating or updating the artifacts, launch `/research-plans:board` automatically so the researcher immediately sees the tracker, instead of requiring a separate command.
