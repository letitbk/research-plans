# v0.20b Autopilot Implementation Plan (2 of 2) — rev 2

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close the loop — one execute prompt after finalize, then capture/validation/report/bookkeeping/next-steps run without human stops, interrupting only when validation flags deviations.

**Architecture:** Spec = `docs/specs/2026-07-16-flow-streamlining-design.md`, Release 2, S2/S3/S5. **Depends on plan A** (`docs/plans/2026-07-17-v0.20a-machinery.md`) complete on the same branch `worktree-v0.20-flow`. **Rev 2** folds the codex review (`docs/specs/2026-07-17-codex-review-v0.20-plans.md`, BK "fold all"): the deviation stop operates on the STAGED bundle and finalizes once (no phantom `rN+1`), revise-plan rebinds staged provenance, autopilot caller modes suppress results.md/report.md's own commit prompts and board opens, "finalize mechanics" narrowed to finalize+verify only, multi-component commit/board aggregation deferred to the end, concrete headless flags (`--go`, `--rerun`), tracker `in progress` set at execution start (plan A moved finalize to `planned`), the zero-artifact row's summary-only-retrospective exception, and the shared `[Unreleased]` changelog section.

**Tech Stack:** prompt-markdown (commands/references), React + TypeScript + vitest (small), Python (small).

## Global Constraints

Identical to plan A (same worktree/branch, cd + branch guards, explicit `git add`, `npm run build` only in Task 8, local vitest, pytest, `tsc -b`, no formatter, no hard-wrap, versions stay 0.19.1). Plus:

- **`CHANGELOG.md [Unreleased]` was CREATED by plan A Task 14** — Task 9 here merges bullets under its existing headings; never add a second `[Unreleased]` or duplicate `### Added`/`### Changed` headings.
- Command `description:` lines are unconditional per-session token cost: `execute.md`'s stays one lean line; the runbook lives in `references/execution-loop.md`, referenced never duplicated.
- The outcome-matrix tracker strings are exactly plan A's three additions (`done (validated)` / `done (unvalidated)` / `done (retrofit)`); Task 7 records the simplification in the spec.

---

### Task 1: `(auto-captured)` decision-log support (code)

**Files:**
- Modify: `board/src/lib/parse.ts:195-217`, `board/src/lib/types.ts:314-319`, `board/src/views/Timeline.tsx:230-238`
- Test: `board/src/lib/parse.test.ts`, `board/src/views/Timeline.outline.test.tsx` (or a new `Timeline.badge.test.tsx`)

- [ ] **Step 1: Failing tests**

```ts
it("flags auto-captured decision entries", () => {
  const md = "# Decision log\n\n## 2026-07-17 14:05 (auto-captured)\nContext: x\n";
  const entries = parseDecisionLog(md);
  expect(entries[0].autoCaptured).toBe(true);
  expect(entries[0].lateCaptured).toBe(false);
});
```

Timeline: an `(auto-captured)` entry renders the badge `auto-captured`.

- [ ] **Step 2: Implement** — `parse.ts:211` area: add `autoCaptured: /auto-captured/i.test(m[2]),`; `types.ts` ParsedLogEntry: `autoCaptured: boolean;`; `Timeline.tsx:235`:

```ts
        badge: entry.lateCaptured
          ? "late-captured at sync"
          : entry.autoCaptured
            ? "auto-captured"
            : undefined,
```

- [ ] **Step 3: Run, commit**

```bash
cd <wt>/board && ./node_modules/.bin/vitest run && ./node_modules/.bin/tsc -b
cd <wt> && git add board/src/lib/parse.ts board/src/lib/types.ts board/src/lib/parse.test.ts board/src/views/Timeline.tsx board/src/views/Timeline.outline.test.tsx
git commit -m "feat(board): auto-captured decision-log label parsed and badged"
```

---

### Task 2: Manifest `curatedBy` (code, small)

**Files:**
- Modify: `skills/managing-research-plans/scripts/results.py` (only if `validate_staged` `:263-336` rejects unknown manifest keys — read it first; if tolerant, py change is test-only), `board/src/lib/types.ts` (ResultsManifest), `board/src/views/Results.tsx` (provenance section)
- Test: `tests/test_results.py` (tolerance), `board/src/views/Results.state.test.tsx` (render)

- [ ] **Step 1:** py test: a staged manifest with `"curatedBy": "agent"` finalizes cleanly. **Step 2:** `types.ts` ResultsManifest: `curatedBy?: "agent";`; Results provenance section renders `curated by agent (autopilot)` when present. **Step 3:** run both suites, commit:

```bash
cd <wt> && git add skills/managing-research-plans/scripts/results.py tests/test_results.py board/src/lib/types.ts board/src/views/Results.tsx board/src/views/Results.state.test.tsx
git commit -m "feat(results): curatedBy provenance for autopilot-curated bundles"
```

---

### Task 3: `references/execution-loop.md` — the autopilot runbook (new file)

The single source of truth after finalize. `execute.md`, `plan.md`'s tail, and `results.md`'s autopilot mode POINT here; none duplicates it.

**Files:**
- Create: `skills/managing-research-plans/references/execution-loop.md`

- [ ] **Step 1: Write the file** (full content):

```markdown
# The execution loop

Referenced by `/research-plans:execute` and the post-finalize chain in `/research-plans:plan`. One human question starts execution; after that, capture, validation, report, and bookkeeping run without stopping — the only interruption is the deviation stop below. `/research-plans:sync` remains the manual recovery checkpoint for work done outside this loop.

## The execute prompt

Ask ONE AskUserQuestion (never re-ask pieces separately) with three parts:

1. **Execute now, or later?** A yes includes consent to COMMIT the signed plan (and its scorecard) first — say so in the question text: the rubric's `uncommitted` integrity flag stays meaningful only when the plan is committed before the work it governs. On "later", stop here; `/research-plans:execute <component>` re-enters this prompt any time.
2. **Which model?** Pre-select the profile's `execute` row (`models.py stage execute`); offer the standard alternatives (opus / sonnet / haiku / a custom id). When the choice differs from the model you are running as, print the standard nudge line and WAIT for the researcher to switch — they opted into that model, so waiting is correct here (the only place the nudge blocks).
3. **Generate a shareable report when done?** One answer covers every component in this batch. Default yes when the component's previous bundle has a report.

On yes: commit the plan (`plan: <NN-slug> v<N>` + the scorecard file), **set the component's tracker row to `in progress`** (execution start owns this transition — finalize left it `planned`), then execute.

## During execution

Unchanged doctrine — the plan resolves the big choices, but interpretive choices that arise mid-run still surface to the researcher before acting (CLAUDE.md rule 4), and decisions append to the log in real time. Multiple components run SEQUENTIALLY in this session — never parallel, never dispatched to subagents.

## The per-component tail (after the work of one component ends)

Run steps 1–3 per component, without asking anything. Steps 4–5 (report + tracker/log bookkeeping) also run per component. The COMMIT SUGGESTION and the BOARD OPEN do not — they are deferred to **After all components** below.

1. **Capture — agent-curated, into staging.** Follow `/research-plans:results <component>` steps 2, 4, 5 in autopilot mode (results.md names it): candidates from the plan's Verification section, this session's outputs, and `discover`; you author titles, captions, and finding statements, grouped under findings exactly as the interview would have; stamp `"curatedBy": "agent"` into the staged manifest. Every other capture rule holds verbatim (provenance rule, producedBy honesty, journal-ready renders, late flags). Say in the capture summary that the researcher's remedy for a mis-curated bundle is Reopen → recapture.
2. **Validate the STAGED bundle** (results.md step 6 verbatim — integrity is computed at finalize; plan-conformance runs now, mechanical status derivation). Nothing is finalized yet.
3. **Branch on the outcome matrix** (below). `deviations-found` → the deviation stop; anything else → **finalize**: run results.md step 7's finalize mechanics ONLY — `results.py finalize` + verify the printed `rN` exists on disk. The report offer, decision-log entry, board open, and commit suggestion in that step are all owned by THIS runbook: skip them there.
4. **Report** — only if the execute prompt pre-answered yes AND the bundle has substantive findings (the null-result gate holds): proceed into `/research-plans:report <component> r<N>` in autopilot mode (report.md names it: write the files and return — no commit suggestion, no board offer). The marker records the validation state (schemaVersion 2).
5. **Bookkeeping (the tail carries these /sync jobs):** tracker row per the matrix + a one-line outcome note (amendment or unverifiable detail goes in the note); decision-log entries for session decisions never logged, each headed `## <YYYY-MM-DD HH:MM> (auto-captured)` — amending one later means APPENDING a corrective entry; `results.py changed` for source drift; a component grown past `references/split-criteria.md` → propose the split as tracker rows (no dialogue).
   NOT carried (manual `/sync` only): hosted-comment pulls, adoption-cutoff handling, no-git evidence handling, reconciliation of work done outside this loop.

## After all components

- **One commit suggestion** covering every bundle, report, tracker row, and log entry (`plans: executed — <slugs>; r<N> captured, validated` — do not run without approval). The ONLY commit prompt in the loop.
- **One board open, view-only:** proceed into the full `/research-plans:board <NN-slug>:r<N>` workflow (the first captured bundle; the researcher navigates to the rest). Routing stays live for comments/reopen; nothing waits on a verdict.
- **Loop closure:** propose next steps from the tracker in one short message — the next `not started` row(s) in table order; several ready → offer batch planning (`/research-plans:plan <a> <b> …`); everything done/dropped → point at `/research-plans:renew`. Never start the next component without the researcher's word.

## The deviation stop (the loop's only interruption)

Trigger: validation status `deviations-found` on the STAGED bundle. Nothing has been finalized; write NO tracker status yet. Present the deviating steps/criteria with their evidence, then ask ONE AskUserQuestion with three remedies — each ends by continuing the tail at step 3's finalize:

- **Revise the plan** — draft `v<N+1>` documenting the deviation (sync.md step 6 mechanics: Supersedes line, rp-model marker, no trailer), score the draft, finalize it in the review room. Then REBIND the staged bundle to the new plan: update the staged `manifest.planVersion` and `validation.planVersion` to `<N+1>`, set `trigger` to `"plan-revision"`, re-run validation against `v<N+1>` (it should now conform), and continue to finalize — ONE bundle, correctly attributed.
- **Fix the work** — treat the deviations as defects: fix scripts, re-run, refresh the staged artifacts/scripts/manifest (re-stage what changed), re-validate, continue to finalize.
- **Accept and log** — append a decision-log entry (real timestamp, the deviation and why it stands); finalize with `deviations-found` sealed; tracker row `done (unvalidated)` with the note naming the accepted deviation; continue the tail from step 4.

## Outcome matrix (normative)

| Validation outcome | Tracker status | Tail behavior |
|---|---|---|
| `conforms` | `done (validated)` | continue |
| `conforms-with-amendments` | `done (validated)` — note names the amendment | continue |
| `deviations-found` | none until the deviation stop resolves | STOP (above) |
| `unverifiable` | `done (unvalidated)` — note says why | continue; surfaced on the board |
| `skipped` (opt-out / headless) | `done (unvalidated)` | continue |
| `not-applicable` (retrofit) | `done (retrofit)` | continue |
| integrity failed | (orthogonal — any row above) | advisory badge; never blocks |
| zero qualifying artifacts | no bundle | results.md rule: report honestly and stop — EXCEPT the explicitly-confirmed summary-only retrospective bundle (results-adopt.md, Summary-only bundles), which remains legitimate |

## Headless rules

No AskUserQuestion means: `/research-plans:execute` never auto-runs. Headless invocation requires explicit flags — `--go` (the authorization; without it, print what is needed and stop), `--model <id>` (optional; absent → profile default, no nudge wait), `--report yes|no` (required with `--go`), `--rerun` (required to re-execute a component whose current bundle already exists; without it such a component is skipped with a note). A mid-run interpretive choice that the plan does not resolve → STOP and report the choice; never decide it headless. The deviation stop cannot ask either: record the deviations, finalize nothing, write no tracker status, report, and stop — never auto-pick a remedy. Validation follows results.md's headless rule (`skipped` when the Task tool is unavailable).
```

- [ ] **Step 2: Commit**

```bash
cd <wt> && git add skills/managing-research-plans/references/execution-loop.md
git commit -m "feat(flow): execution-loop runbook — execute prompt, staged-bundle tail, deviation stop, outcome matrix"
```

---

### Task 4: `commands/execute.md` (new, thin)

**Files:**
- Create: `commands/execute.md`
- Test: `tests/test_command_docs.py` (add execute.md to its inventory checks)

- [ ] **Step 1: Write the command** (full content):

```markdown
---
description: Execute one or more signed plans — one question, then the loop runs to results, validation, and report
argument-hint: [component names/numbers... | --go --report yes|no [--model <id>] [--rerun] (headless)]
allowed-tools: Read, Write, Edit, Glob, Grep, AskUserQuestion, Task, Bash(python3:*), Bash(python:*), Bash(Rscript:*), Bash(bash:*), Bash(tee:*), Bash(mkdir:*), Bash(git:*), Bash(ls:*), Bash(date:*)
---

Execute signed execution plans and run the full loop after them. Skill context: `${CLAUDE_PLUGIN_ROOT}/skills/managing-research-plans/SKILL.md`. The whole choreography lives in `${CLAUDE_PLUGIN_ROOT}/skills/managing-research-plans/references/execution-loop.md` — load it and follow its named sections; this command adds only the entry checks below. Requires an initialized project (`plans/master-plan.md` with its marker); if absent, say so and stop.

1. **Resolve the components.** Arguments starting with `--` are flags (headless — see **Headless rules** in the reference), never component names. Each remaining entry names a component (name or number via the master plan tracker). No component argument → propose the first `planned` row and confirm inside the execute prompt. Per component: unknown → error naming the valid rows, before anything runs; no signed plan → point at `/research-plans:plan <component>` (an unsigned draft belongs in the review room first); already executed with a current bundle → interactive: say so and ask re-run vs skip inside the execute prompt; headless: skip unless `--rerun`.

2. **The execute prompt.** Follow **The execute prompt** in the execution loop reference — one question covering now/later, model, and the report preference, with plan-commit consent inside the "now" option. Headless sessions follow **Headless rules** instead (`--go` authorizes; absent → print what is needed and stop).

3. **Execute and run the tail.** For each component in order: do the work under the plan (**During execution**), then **The per-component tail** — capture into staging, validate, branch on the **Outcome matrix** (the **deviation stop** interrupts on deviations), finalize, report when pre-answered, bookkeeping. Then **After all components** — the single commit suggestion, the one view-only board, and **Loop closure**.
```

- [ ] **Step 2: Doc tests + commit**

```bash
cd <wt> && python3 -m pytest tests/test_command_docs.py -q
git add commands/execute.md tests/test_command_docs.py
git commit -m "feat(flow): /research-plans:execute — thin entry over the execution-loop runbook"
```

---

### Task 5: Chain rewiring — plan.md tail, sync.md division, board.md post-approve

**Files:**
- Modify: `commands/plan.md` (steps 7–8), `commands/sync.md` (intro + step 7), `commands/board.md` (`:33`)

- [ ] **Step 1: plan.md** — step 7: "7. **Commit rides the execute prompt.** Do not suggest a separate plan commit here — consent to commit the signed plan is part of the execute prompt (next step)." Step 8 (plan A's version) → "**8. Offer execution.** After the review-room approve is routed (`v1.md` signed, scorecard migrated, tracker `planned`), proceed into the execute prompt — load `${CLAUDE_PLUGIN_ROOT}/skills/managing-research-plans/references/execution-loop.md` and follow **The execute prompt** (a multi-component batch offers the whole approved set). On 'later', say `/research-plans:execute <component>` re-enters it and stop."
- [ ] **Step 2: sync.md** — intro (line 6) gains: "This is the MANUAL checkpoint — the primary loop (`/research-plans:execute`) runs capture, validation, and bookkeeping itself; `/sync` covers work done outside that loop, crashed sessions, hosted-comment pulls, and adoption-cutoff reconciliation." Step 7 gains: "Skip any component the execution loop already captured this session (its tail owns the bundle and the board open)."
- [ ] **Step 3: board.md:33** — append to the approve bullet: "When this approve finalized a plan in a live interactive session, continue per plan.md step 8: offer the execute prompt for the newly-signed component (execution-loop reference, **The execute prompt**)."
- [ ] **Step 4: Doc tests + commit**

```bash
cd <wt> && python3 -m pytest tests/test_command_docs.py -q
git add commands/plan.md commands/sync.md commands/board.md
git commit -m "feat(flow): finalize chains into the execute prompt; /sync framed as the manual checkpoint"
```

---

### Task 6: results.md + report.md autopilot caller modes

Codex P1: without explicit modes, results.md's own commit suggestion and board open (and report.md's) would still fire inside the loop.

**Files:**
- Modify: `commands/results.md` (`:13`, `:15`, `:21`, `:23`), `commands/report.md` (its return/board-offer step)

- [ ] **Step 1: results.md step 3 mode header** — prepend: "**Two modes.** *Interactive capture* (invoked directly, or chained from `/sync`): the interview below runs as written. *Autopilot capture* (chained from the execution loop): SKIP the interview — curation follows **The per-component tail** step 1 in `${CLAUDE_PLUGIN_ROOT}/skills/managing-research-plans/references/execution-loop.md` (you author titles/captions/findings, stamp `curatedBy: "agent"`; the researcher's remedy is Reopen → recapture); every rule below about producedBy honesty, renders, and metrics-as-findings applies to both modes."
- [ ] **Step 2: results.md step 2** — "Propose; the researcher confirms." gains "(interactive mode; autopilot curates without confirming — see step 3)."
- [ ] **Step 3: results.md step 6** — append: "In the execution loop, this validation runs on the STAGED bundle BEFORE finalize and before any tracker or decision-log bookkeeping — a deviations-found result interrupts per the loop's deviation stop instead of merely echoing."
- [ ] **Step 4: results.md step 7 — autopilot ending** — append: "**Autopilot mode ends here:** when chained from the execution loop, step 7 is finalize + on-disk verification ONLY — the report offer, the decision-log entry, the board open, and the commit suggestion are all owned by the loop's runbook; skip every one of them and return."
- [ ] **Step 5: report.md** — in its return step: "…when this run was chained from `/research-plans:results`, just return" gains "— likewise when chained from the execution loop: write the files and return; no commit suggestion, no board offer (the loop owns both)."
- [ ] **Step 6: Doc tests + commit**

```bash
cd <wt> && python3 -m pytest tests/test_command_docs.py -q
git add commands/results.md commands/report.md
git commit -m "feat(results,report): autopilot caller modes — the loop owns commits, logs, and board opens"
```

---

### Task 7: Doctrine sweep + spec errata

**Files:**
- Modify: `skills/managing-research-plans/SKILL.md` (`:39-40`, stage list, `:86` command table), `skills/managing-research-plans/templates/claude-md-section.md`, `skills/managing-research-plans/references/results-adopt.md` (`:26` "the per-component interview is the verification" sentence — rewrite for the two-mode model), `skills/managing-research-plans/references/split-criteria.md` (grep for verdict/sync-only claims), `QUICKSTART.md` (`:36-41`), `docs/reference.md` (command list + narrative), `README.md` (stale claims only), `docs/specs/2026-07-16-flow-streamlining-design.md` (errata)

- [ ] **Step 1: SKILL.md** — `:39` → "Results capture runs automatically at the end of the execution loop (and from `/research-plans:sync` for out-of-loop work) — in the loop the agent curates the bundle (labeled `curatedBy: agent`) and verification = the mechanical validation pass plus the researcher's standing Reopen right; in a manual `/sync` capture the per-component interview still runs. Either way capture stays visible and evidence-based, never a silent bulk write." `:40` → "`/research-plans:sync` is the manual recovery checkpoint; the primary loop is plan → review room → execute → tail. Use sync's late-capture protocol when logging was missed outside the loop." Command table: add `| /research-plans:execute | One question, then the loop: execute, capture, validate, report, next steps |`.
- [ ] **Step 2: claude-md-section.md** — read first; update any sole-checkpoint `/sync` reference to name the loop; rule 4 verbatim.
- [ ] **Step 3: results-adopt.md:26** — "The per-component interview is the verification; skipping it is how plan theater starts." → "In a manual capture the per-component interview is the verification; in the execution loop the verification is the mechanical validation pass plus the researcher's Reopen right over an agent-curated, `curatedBy`-labeled bundle. Silent bulk writes are how plan theater starts — both modes stay visible and evidence-based." Sweep `split-criteria.md` via grep for anything now false.
- [ ] **Step 4: QUICKSTART + reference.md + README** — QUICKSTART section 4 → the new loop (review room → execute prompt → validated bundle → next-step proposal), `/sync` stays in recovery (`:98`); reference.md adds `/execute` + updates the narrative to the four-stop loop; README: `grep -n "verdict\|sync" README.md` — fix only sentences now false.
- [ ] **Step 5: Spec errata** — append to the spec's Revision history: "- 2026-07-17 rev 2.1 — implementation simplifications (plans v0.20a/b rev 2, codex-reviewed): outcome-matrix tracker strings are exactly `done (validated)` / `done (unvalidated)` / `done (retrofit)`; batch approval binds the displayed text via a server-issued content hash echoed by the client and checked against disk under a lock; scorecard migration lives in review.md's idempotence rule; finalize marks `planned`, execution start marks `in progress`; the deviation stop operates on the staged bundle and finalizes once."
- [ ] **Step 6: Doc tests + commit**

```bash
cd <wt> && python3 -m pytest tests/test_command_docs.py -q
git add skills/managing-research-plans/SKILL.md skills/managing-research-plans/templates/claude-md-section.md skills/managing-research-plans/references/results-adopt.md skills/managing-research-plans/references/split-criteria.md QUICKSTART.md docs/reference.md README.md docs/specs/2026-07-16-flow-streamlining-design.md
git commit -m "docs: doctrine sweep — four-stop loop, /execute, sync as manual checkpoint, spec errata"
```

---

### Task 8: Rebuild + full verification + walkthrough smoke + token check

- [ ] **Step 1: Full suites** — vitest + tsc + pytest green.
- [ ] **Step 2: Rebuild + commit template** — `npm run build`; `grep -c "auto-captured" <wt>/skills/managing-research-plans/assets/board-template.html` ≥ 1; commit the template.
- [ ] **Step 3: Walkthrough smoke** (scratch project): full loop — `/plan` dialogue → review room approve (tracker `planned`) → execute prompt (now + report yes; plan commit rides it; tracker `in progress`) → trivial execution → tail: staged capture with `curatedBy: agent` → validation conforms → finalize → report with v2 marker → tracker `done (validated)` → auto-captured entries badged → ONE commit suggestion at the end → board view-only → next-step proposal. Deviation branch: outputs miss a criterion → stop offers three remedies → pick accept-and-log → bundle finalizes with deviations-found sealed → tracker `done (unvalidated)` with the note. Headless: `claude -p` with no `--go` → prints requirements and stops.
- [ ] **Step 4: Token check** — `wc -c commands/*.md` vs branch base; the `description:` line sum must not exceed baseline by more than ~120 bytes; report the numbers.

```bash
cd <wt> && git add skills/managing-research-plans/assets/board-template.html
git commit -m "build(board): ship v0.20b autopilot template"
```

---

### Task 9: CHANGELOG — merge into the existing `[Unreleased]`

- [ ] Merge bullets under plan A's existing `### Added` / `### Changed` headings (never a second `[Unreleased]`):

Under `### Added`:

```markdown
- **`/research-plans:execute`** — one question after a plan is signed (run now? which model? report at the end?) and the loop runs itself: agent-curated capture (labeled), validation before any bookkeeping, report, tracker and decision-log updates, one commit suggestion, a view-only board, and a next-step proposal. Validation that finds deviations stops the loop with three concrete remedies on the still-staged bundle; nothing else interrupts.
- Auto-captured decision-log entries (`(auto-captured)` label) parsed and badged on the Timeline.
```

Under `### Changed`:

```markdown
- `/research-plans:sync` is now the manual recovery checkpoint (out-of-loop work, hosted-comment pulls, crashed sessions); the primary path is plan → review room → execute.
```

```bash
cd <wt> && git add CHANGELOG.md && git commit -m "docs: changelog for v0.20b autopilot"
```

---

## Self-review notes (spec + review coverage)

- Codex P1 folds: staged-bundle deviation semantics + provenance rebind + `trigger: plan-revision` (T3); finalize-mechanics narrowed (T3 step 3, T6 step 4); per-component vs after-all aggregation explicit (T3, T4); headless `--go`/`--rerun`/flag-vs-component parsing + interpretive-choice stop (T3, T4); results.md/report.md caller modes suppressing their commit prompts and board opens (T6); tracker `in progress` at execution start (T3, matching plan A's finalize→`planned`); zero-artifact summary-only-retrospective exception (T3 matrix); results-adopt.md + split-criteria.md in the sweep (T7); single shared `[Unreleased]` (T9).
- Cross-plan contracts: tracker strings = plan A's union exactly; v2 markers = plan A Task 4; `curatedBy` = Task 2; plan.md/sync.md/board.md edits apply A-then-B and compose.
- Out of scope unchanged: parallel execution, chronological template, screenshots (release cut).
